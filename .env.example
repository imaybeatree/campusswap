# Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=campusswap

# Server
PORT=3001
JWT_SECRET=change-this-to-a-random-string

# Client (leave empty in production — same origin)
VITE_BACKEND_URL=http://localhost:3001
