services:
  db:
    image: mysql:8.0
    container_name: campusswap-db
    restart: unless-stopped
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: campusswap
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
